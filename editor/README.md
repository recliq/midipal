MIDIpal-editor is a python 3 AppEngine web app for programming MIDI filters on the MIDIpal. Configure a filter/remapping, and grab a .syx file to send to the MIDIpal!

It’s back online here:

https://midipal-editor-350009.ew.r.appspot.com 2

To run the editor locally from the root of the midipal git repo:
```
python3 -m venv env
source env/bin/activate
cd editor
pip install -r requirements.txt
python main.py
```
